{% extends 'base.html.twig' %}

{% block title %}Equipes{% endblock %}

{% block body %}
    <h1 class="ptn_title">Equipes</h1>
    </div>
    <p class="ptn_subtitle"> <span class="red bold">TEDx</span> QuaideSaintOuen existe grâce au travail et aux efforts collectifs de notre équipe.<br>
        Ensemble, nous créons des idées qui méritent d'être diffusées.</p>

        {% for team in teams %}
            <div class="team_ctn col-3">
                <img src="{{ team.photo }}">
                <p class="team_name">{{ team.name }}</p>
                <p class="team_job">  {{ team.job }}</p>
            </div>
        {% endfor %}


    {% endblock %}

{{ form_start(form, {'attr': {'novalidate': 'novalidate', 'class': 'form-contact'}}) }}
    <div>
        {{ form_errors(form) }}
    </div>
    <div>
        {{ form_row(form.subject, {'label': 'Sujet', 'attr': {'class': 'form-contact'}}) }}
    </div>
    <div>
        {{ form_row(form.name, {'label': 'Nom', 'attr': {'class': 'form-contact'}}) }}
    </div>
    <div>
        {{ form_row(form.email, {'label': 'Email', 'attr': {'class': 'form-contact'}}) }}
    </div>
    <div class="message">
        {{ form_row(form.message, {'label': 'Message', 'attr': {'class': 'form-contact'}}) }}
    </div>

    <button class="btn">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}